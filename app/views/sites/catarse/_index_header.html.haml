= content_for :content_header do
  #index_header
    #index_header_text
      %h1= t('.title')
      %h2= t('.subtitle_html')
    #index_share= render "/sites/#{current_site.path}/index_share"
    .clear
