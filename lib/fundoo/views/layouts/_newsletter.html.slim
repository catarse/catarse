= form_tag CatarseSettings['mailchimp_url'], id: 'mailee-form', method: 'post', class: "newsletter" do
  = email_field_tag 'EMAIL', nil, placeholder: t('projects.index.sidebar.placeholders.email'), label: 'email', class: 'newsletter-input'
  = submit_tag t('projects.index.sidebar.buttons.newsletter'), class: 'newsletter-submit'
