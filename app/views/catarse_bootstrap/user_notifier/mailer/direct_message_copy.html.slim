- message = @notification.direct_message

|Olá <strong>#{@notification.direct_message.from_name}</strong>
br

|Esta é a mensagem que você enviou para o #{link_to message.project.try(:user).try(:display_name), user_url(message.project.try(:user))}, através da página do #{link_to message.project.name, project_url(message.project)}:
br
br
p == message.content

p Ao responder este email, <strong>você estará enviando uma mensagem diretamente para o endereço #{message.project.try(:user).try(:email)}, que é o email de #{message.project.try(:user).try(:display_name)}</strong>.

p <strong>Importante</strong>: O Catarse não se responsabiliza pelo conteúdo das mensagens enviadas através da plataforma. Caso entenda que o conteúdo da mensagem se trata de spam ou que infringe de alguma forma os nossos Termos de Uso, pedimos que avise a nossa equipe através do email: #{mail_to CatarseSettings[:email_contact]}.
