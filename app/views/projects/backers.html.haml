= content_for :content_header do
  %h1 Apoiadores do #{link_to truncate(@project.name, :length => 32), project_path(@project)}
  %h2 Este é o grupo que faz de tudo para este projeto acontecer. Faça parte você também!
#project_description
  #backers= render :partial => 'backers', :locals => {:show_all_link => false, :show_badge => true, :show_rewards => true}
= render :partial => 'back_info', :locals => {:show_backers => false}

