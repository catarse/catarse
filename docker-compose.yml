---
catarse:
    image: ruby-catarse
    ports:
        - 3000
    volumes:
        - .:/app/
    links:
        - postgres
        - redis
postgres:
    image: postgres
    ports:
        - 5432
redis:
    image: redis
    ports:
        - 6379
