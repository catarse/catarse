%br
.project_list_header= t("sites.#{current_site.path}.projects.#{quick}.title")
.project_list_subheader= t("sites.#{current_site.path}.projects.#{quick}.subtitle")
.project_list_wrapper
  .project_list
    = render :partial => 'project', :collection => collection
.explore_more
  %hr
  = link_to explore_quick_path(quick) do
    = t(".explore_#{quick}")
    .explore_more_arrow >

